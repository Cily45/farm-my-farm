package models.vegetable.vegetable;

import models.StartOrganism;

public class Seed extends StartOrganism {
    public Seed(String type, int price, int quantity) {
        super(type, price, quantity);
    }
}
